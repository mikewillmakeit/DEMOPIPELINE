pipeline {
    agent LINUXNODE
    stages{
        stage('TerraformPlan'){
            steps { 
                powershell "teraform init"
                powershell "terraform apply -state=path /example.tf" 
            }
        stage('Pester Test')   
        }
    }
}